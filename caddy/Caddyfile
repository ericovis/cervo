# cervo's front door: cervo itself, and nothing else. Hosted sites are
# published into the running config by the worker over the admin API — see
# src/cervo/caddy.py. Placeholders come from the container's environment.
{
	admin 0.0.0.0:2019
	email "{$ACME_EMAIL}"
}

{$SCHEME:http}://{$DOMAIN:localhost} {
	reverse_proxy app:8000
}
